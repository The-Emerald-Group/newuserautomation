name: Build and Release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Resolve release version
        id: release_version
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          $version = $tag.TrimStart('v', 'V')
          "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Publish app (win-x64)
        shell: pwsh
        run: |
          ./scripts/publish.ps1 -Runtime win-x64 -Configuration Release -Version "${{ steps.release_version.outputs.version }}" -ZipOutput

      - name: Publish installer EXE (win-x64)
        shell: pwsh
        run: |
          dotnet publish ./scripts/InstallerLauncher/InstallerLauncher.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -p:Version=${{ steps.release_version.outputs.version }} `
            -o ./dist/installer

      - name: Publish GitHub release assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/NewUserAutomation-win-x64.zip
            dist/installer/NewUserAutomationInstaller.exe
